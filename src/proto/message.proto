syntax = "proto3";

message Message {
  string service = 1;
  bytes content = 2;
}

message BotProtocol {
  string key = 1;
}

message BotResponse {
  ErrorCode error = 1;

  enum ErrorCode {
    DATABASE = 0;
  }
}
